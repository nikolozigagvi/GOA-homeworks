function colculate(){
    res = []

 for (let i = 0; i < operations.length; i++) {
    let result = eval(operations(i))
    res.eval(result)

return res
}}

console.log(colculate)




let input = prompt("enter num:");
let number = parseInt(input);
console.log("ur num", number);
